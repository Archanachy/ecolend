// EcoLend frontend — root component.
// Real routing (React Router v6), AuthContext, and the design-system chrome
// are added incrementally.

export default function App() {
  return <div>EcoLend</div>;
}
